# cccs

Cccs - JavaScript client for cccs
No description provided (generated by Swagger Codegen https://github.com/swagger-api/swagger-codegen)
This SDK is automatically generated by the [Swagger Codegen](https://github.com/swagger-api/swagger-codegen) project:

- API version: 0.1
- Package version: 0.1
- Build package: io.swagger.codegen.languages.JavascriptClientCodegen

## Installation

### For [Node.js](https://nodejs.org/)

#### npm

To publish the library as a [npm](https://www.npmjs.com/),
please follow the procedure in ["Publishing npm packages"](https://docs.npmjs.com/getting-started/publishing-npm-packages).

Then install it via:

```shell
npm install cccs --save
```

##### Local development

To use the library locally without publishing to a remote npm registry, first install the dependencies by changing 
into the directory containing `package.json` (and this README). Let's call this `JAVASCRIPT_CLIENT_DIR`. Then run:

```shell
npm install
```

Next, [link](https://docs.npmjs.com/cli/link) it globally in npm with the following, also from `JAVASCRIPT_CLIENT_DIR`:

```shell
npm link
```

Finally, switch to the directory you want to use your cccs from, and run:

```shell
npm link /path/to/<JAVASCRIPT_CLIENT_DIR>
```

You should now be able to `require('cccs')` in javascript files from the directory you ran the last 
command above from.

#### git
#
If the library is hosted at a git repository, e.g.
https://github.com/GIT_USER_ID/GIT_REPO_ID
then install it via:

```shell
    npm install GIT_USER_ID/GIT_REPO_ID --save
```

### For browser

The library also works in the browser environment via npm and [browserify](http://browserify.org/). After following
the above steps with Node.js and installing browserify with `npm install -g browserify`,
perform the following (assuming *main.js* is your entry file, that's to say your javascript file where you actually 
use this library):

```shell
browserify main.js > bundle.js
```

Then include *bundle.js* in the HTML pages.

### Webpack Configuration

Using Webpack you may encounter the following error: "Module not found: Error:
Cannot resolve module", most certainly you should disable AMD loader. Add/merge
the following section to your webpack config:

```javascript
module: {
  rules: [
    {
      parser: {
        amd: false
      }
    }
  ]
}
```

## Getting Started

Please follow the [installation](#installation) instruction and execute the following JS code:

```javascript
var Cccs = require('cccs');

var defaultClient = Cccs.ApiClient.instance;

// Configure API key authorization: apiKeyHeader
var apiKeyHeader = defaultClient.authentications['apiKeyHeader'];
apiKeyHeader.apiKey = "YOUR API KEY"
// Uncomment the following line to set a prefix for the API key, e.g. "Token" (defaults to null)
//apiKeyHeader.apiKeyPrefix['X-API-KEY'] = "Token"

var api = new Cccs.MediaApi()

var attachment = "/path/to/file.txt"; // {File} The file to be uploaded

var opts = { 
  'media': new Cccs.Media() // {Media} 
};

var callback = function(error, data, response) {
  if (error) {
    console.error(error);
  } else {
    console.log('API called successfully.');
  }
};
api.apiMediaCreate(attachment, opts, callback);

```

## Documentation for API Endpoints

All URIs are relative to *https://localhost/api/v1*

Class | Method | HTTP request | Description
------------ | ------------- | ------------- | -------------
*Cccs.MediaApi* | [**apiMediaCreate**](docs/MediaApi.md#apiMediaCreate) | **POST** /media | 
*Cccs.ProjectsApi* | [**apiProjectCreate**](docs/ProjectsApi.md#apiProjectCreate) | **POST** /projects | Post a project
*Cccs.ProjectsApi* | [**apiProjectDelete**](docs/ProjectsApi.md#apiProjectDelete) | **DELETE** /projects/{id} | Remove a project
*Cccs.ProjectsApi* | [**apiProjectGet**](docs/ProjectsApi.md#apiProjectGet) | **GET** /projects | Get all projects
*Cccs.ProjectsApi* | [**apiProjectGetOne**](docs/ProjectsApi.md#apiProjectGetOne) | **GET** /projects/{id} | Get a single project
*Cccs.ProjectsApi* | [**apiProjectPut**](docs/ProjectsApi.md#apiProjectPut) | **PUT** /projects/{id} | Modify/Create a project
*Cccs.SubmissionsApi* | [**apiSubmissionCreate**](docs/SubmissionsApi.md#apiSubmissionCreate) | **POST** /submissions | Post a submission
*Cccs.SubmissionsApi* | [**apiSubmissionDelete**](docs/SubmissionsApi.md#apiSubmissionDelete) | **DELETE** /submissions | Remove a submission
*Cccs.SubmissionsApi* | [**apiSubmissionGet**](docs/SubmissionsApi.md#apiSubmissionGet) | **GET** /submissions | Get a single submission
*Cccs.SubmissionsApi* | [**apiSubmissionGetOne**](docs/SubmissionsApi.md#apiSubmissionGetOne) | **GET** /submission/{id} | Get a single submission
*Cccs.SubmissionsApi* | [**apiSubmissionPut**](docs/SubmissionsApi.md#apiSubmissionPut) | **PUT** /submissions | Modify/Create a submission
*Cccs.TasksApi* | [**apiTaskCreate**](docs/TasksApi.md#apiTaskCreate) | **POST** /tasks | Post a task
*Cccs.TasksApi* | [**apiTaskDelete**](docs/TasksApi.md#apiTaskDelete) | **DELETE** /tasks | Remove a task
*Cccs.TasksApi* | [**apiTaskGet**](docs/TasksApi.md#apiTaskGet) | **GET** /tasks | Get a single task
*Cccs.TasksApi* | [**apiTaskGetOne**](docs/TasksApi.md#apiTaskGetOne) | **GET** /tasks/{id} | Get a single task
*Cccs.TasksApi* | [**apiTaskPut**](docs/TasksApi.md#apiTaskPut) | **PUT** /tasks | Modify/Create a task
*Cccs.UsersApi* | [**apiOtokenGenerate**](docs/UsersApi.md#apiOtokenGenerate) | **POST** /users/authorize | Post auth for token response
*Cccs.UsersApi* | [**apiUserGet**](docs/UsersApi.md#apiUserGet) | **GET** /users/{id} | Get all users (or those matching an ID)
*Cccs.UsersApi* | [**apiUserGetUsers**](docs/UsersApi.md#apiUserGetUsers) | **GET** /users | 
*Cccs.UsersApi* | [**apiUserLogin**](docs/UsersApi.md#apiUserLogin) | **POST** /users/login | Allow a user to login
*Cccs.UsersApi* | [**apiUserRegister**](docs/UsersApi.md#apiUserRegister) | **POST** /users/register | Registration end point for a user account
*Cccs.UsersApi* | [**decoratorsAccessChecksEnsureKey**](docs/UsersApi.md#decoratorsAccessChecksEnsureKey) | **GET** /users/validate | OAuth2 token info


## Documentation for Models

 - [Cccs.Media](docs/Media.md)
 - [Cccs.Otoken](docs/Otoken.md)
 - [Cccs.Project](docs/Project.md)
 - [Cccs.Submission](docs/Submission.md)
 - [Cccs.Task](docs/Task.md)
 - [Cccs.User](docs/User.md)


## Documentation for Authorization


### apiKeyHeader

- **Type**: API key
- **API key parameter name**: X-API-KEY
- **Location**: HTTP header

### oauth2

- **Type**: OAuth
- **Flow**: implicit
- **Authorization URL**: http://localhost:8081/oauth/authorize
- **Scopes**: 
  - apiKey: Unique identifier of the user accessing the service.

